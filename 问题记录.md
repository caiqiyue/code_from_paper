# 问题记录

更新时间：2026-04-09

本文档根据近期 Codex 会话记录、仓库内审核文档、以及当前代码状态整理，聚焦两个项目在实验执行中真实遇到过或已经被确认的关键问题：

- `PrE-Text`
- `thesis_platform`（创新算法）

记录格式统一为：

- `问题现象`
- `根本原因`
- `解决方式`
- `当前状态`
- `证据来源`

## 一、创新算法 `thesis_platform`

### 1. FL 主流程结束后进入下游评估时可能 OOM

- 问题现象
  - 联邦训练阶段结束后，切换到 downstream evaluation / cross-domain evaluation 时，GPU 显存不干净，容易在 `22GB RTX 2080 Ti` 上触发 OOM。
- 根本原因
  - 早期问题不在于单纯 CUDA cache，而在于 `server_ctx`、`client_contexts` 及各类 adapter 仍然持有实际 GPU 对象。
  - 具体包括：
    - `server_ctx.text_backend`
    - `client_contexts[*].text_backend`
    - `client_contexts[*].embedder`
    - `generator`
    - `scorer`
    - `retriever`
    - `critic`
    - `aggregator`
  - 仅调用 `_clear_gpu_memory()` 只能清理缓存，不能释放这些对象本身持有的显存。
- 解决方式
  - 在 `thesis_platform/core/experiment_runner.py` 中加入并调用 `_release_fl_resources(...)`，在下游评估前显式释放 FL 阶段对象。
  - 使用 `release_component_resources(...)` 递归调用对象的 `release()`，再执行 `torch.cuda.empty_cache()` 和 `torch.cuda.ipc_collect()`。
  - 当前流程已变为：
    1. FL rounds 完成
    2. `_release_fl_resources(...)`
    3. `_clear_gpu_memory()`
    4. 进入 downstream eval / cross-domain eval
- 当前状态
  - 已修复，且代码和正式实验文档都已明确这一约束。
- 证据来源
  - Codex 会话：`C:\Users\Administrator\.codex\sessions\2026\04\08\rollout-2026-04-08T17-33-46-019d6c70-d1b9-7bc2-927f-8dfaece462cb.jsonl`
  - 代码：
    - [experiment_runner.py](/d:/学习记录/导师项目/研究/caiqiyue_file/thesis_platform/core/experiment_runner.py#L477)
    - [resource_cleanup.py](/d:/学习记录/导师项目/研究/caiqiyue_file/thesis_platform/core/resource_cleanup.py#L7)
  - 文档：
    - [服务器资源.md](/d:/学习记录/导师项目/研究/caiqiyue_file/服务器资源.md#L183)
    - [最终正式实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/最终正式实验设计.md#L47)

### 2. 大模型实验不能与常规实验混排，否则显存风险高

- 问题现象
  - `7B/8B` 模型实验在单卡 `22GB` 服务器上显存风险高，若与其他实验并行，极易 OOM 或导致批次阻塞。
- 根本原因
  - 服务器核心约束是单卡 `RTX 2080 Ti 22GB`，而非 CPU 或系统内存。
  - `GC5`、`GM2`、`GM3`、`GM4` 都属于高风险单卡实验，和常规配置叠加时没有安全余量。
- 解决方式
  - 正式实验中将以下实验强制列为独立批次串行执行：
    - `GC5`
    - `GM2`
    - `GM3`
    - `GM4`
  - 文档中明确禁止多个大模型在单次实验或多个实验间同时常驻 GPU。
- 当前状态
  - 已在正式实验设计中收紧，不再作为普通批次混跑。
- 证据来源
  - [服务器资源.md](/d:/学习记录/导师项目/研究/caiqiyue_file/服务器资源.md#L29)
  - [最终正式实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/最终正式实验设计.md#L44)

### 3. 路径与 `repo_root` 传递不稳时，可能导致运行期找不到资源

- 问题现象
  - 运行 `thesis_platform` 时，如果 `repo_root`、模型目录或数据目录解析不一致，可能出现 `File not found`、模型定位失败或 adapter 初始化异常。
- 根本原因
  - 实验配置大量依赖相对路径和动态创建 adapter；若运行入口没有把 `repo_root` 一致传给 backend / adapter，路径解析容易漂移。
- 解决方式
  - 在 `ExperimentRunner` 中统一使用 `self.repo_root = config.repo_root()`，并在构建 backend、embedder、generator、scorer、retriever、critic、aggregator 时统一传入。
  - 测试设计里也把该类问题归入正式启动前必查项。
- 当前状态
  - 当前主线代码已按统一 `repo_root` 传递，风险已显著下降。
- 证据来源
  - Codex 会话：`C:\Users\Administrator\.codex\sessions\2026\03\31\rollout-2026-03-31T10-21-49-019d41b2-7e28-7a33-b4b3-c8540135e776.jsonl`
  - 代码：
    - [experiment_runner.py](/d:/学习记录/导师项目/研究/caiqiyue_file/thesis_platform/core/experiment_runner.py#L66)
    - [experiment_runner.py](/d:/学习记录/导师项目/研究/caiqiyue_file/thesis_platform/core/experiment_runner.py#L927)
  - 文档：
    - [测试实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/测试实验设计.md#L551)

### 4. GLUE 评测找不到语料时，不是模型问题，而是阶段顺序问题

- 问题现象
  - 独立执行 GLUE 评测时，可能报“找不到合成语料”或结果文件缺失。
- 根本原因
  - GLUE 评测依赖前序主实验已经产出 `downstream_eval/stage2/llama7b_text_syn.json` 或等效语料文件。
  - 若直接跑 `run_glue_eval` 而未先跑主实验，就属于阶段顺序错误。
- 解决方式
  - 固定执行顺序：
    1. `run_experiment`
    2. 再执行 `run_glue_eval`
  - 已在测试设计文档中明确记录。
- 当前状态
  - 当前属于已知执行规则，不再是代码 bug。
- 证据来源
  - [测试实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/测试实验设计.md#L258)
  - [测试实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/测试实验设计.md#L553)

## 二、`PrE-Text`

### 5. `eval_large` 在 22GB 单卡上不适合作为正式实验路径

- 问题现象
  - `eval_large` 使用 `llama_2_7b_hf + PEFT LoRA` 时，在 `RTX 2080 Ti 22GB` 上出现失败，测试阶段归因为 dtype 兼容性与大模型资源压力。
- 根本原因
  - `7B` 级大模型本身已接近单卡上限。
  - 叠加 PEFT LoRA、大 batch 或额外阶段时，正式实验稳定性差，不适合作为常规正式路径。
  - 仓库文档中已经明确：问题不在主链逻辑，而在大模型评估路径的资源与兼容性。
- 解决方式
  - 正式实验中直接排除 `eval_large`。
  - 小评估统一走 `GPT-2`。
  - 小模型测试阶段用 `distilgpt2/gpt2` 验证代码链路，而不是继续硬顶 `eval_large`。
- 当前状态
  - 已通过实验设计收敛：`eval_large` 不纳入本轮正式实验。
- 证据来源
  - [PrE-Text/docs/小模型测试实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/PrE-Text/docs/小模型测试实验设计.md#L8)
  - [最终正式实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/最终正式实验设计.md#L44)
  - [服务器资源.md](/d:/学习记录/导师项目/研究/caiqiyue_file/服务器资源.md#L176)

### 6. `PrE-Text` Stage 2 的运行环境曾存在歧义

- 问题现象
  - 早期文档和测试流程中，`PrE-Text` 有时在 `caiqiyue` 环境下跑小模型测试；但正式 Stage 2 需要 `vllm + 本地模型`，如果沿用错误环境，容易出现依赖缺失或 backend 不一致。
- 根本原因
  - `caiqiyue` 环境不包含 `vllm`，而 `pretext` 环境包含 `vllm==0.3.3`。
  - 正式实验与测试实验的环境目标不完全相同，早期容易混淆。
- 解决方式
  - 将正式口径统一为：
    - `PrE-Text` 正式实验全部使用 `pretext`
    - `bootstrap.generator_backend = vllm`
  - 小模型测试只用于验证逻辑，不再作为正式实验环境依据。
- 当前状态
  - 已在正式实验设计与服务器资源文档中统一。
- 证据来源
  - [服务器资源.md](/d:/学习记录/导师项目/研究/caiqiyue_file/服务器资源.md#L89)
  - [服务器资源.md](/d:/学习记录/导师项目/研究/caiqiyue_file/服务器资源.md#L105)
  - [最终正式实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/最终正式实验设计.md#L25)

### 7. `c4_checkpoint.pth` 缺失导致小评估口径不稳定

- 问题现象
  - `PrE-Text` 小评估曾同时存在两种模式：
    - `distilgpt2` warm-start
    - `gpt2`
  - 如果脚本按“是否存在 `c4_checkpoint.pth`”自动切换，会导致正式实验口径随环境变化而漂移。
- 根本原因
  - 服务器上当前没有单独的 `c4_checkpoint.pth`。
  - 旧逻辑里 `run_eval_small.py` 只要检测到 checkpoint，就会自动切换到 `DistilGPT2`，不够可控。
- 解决方式
  - 正式实验文档统一规定小评估口径为 `GPT-2`。
  - 已修改 `PrE-Text/pretext_platform/scripts/run_eval_small.py`，使其严格遵循 `eval_small.eval_mode`：
    - `gpt2` 就固定走 GPT-2
    - `distilgpt2` 才要求 checkpoint，否则直接报错
- 当前状态
  - 已修复，正式实验口径稳定。
- 证据来源
  - [最终正式实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/最终正式实验设计.md#L96)
  - [服务器资源.md](/d:/学习记录/导师项目/研究/caiqiyue_file/服务器资源.md#L136)
  - [run_eval_small.py](/d:/学习记录/导师项目/研究/caiqiyue_file/PrE-Text/pretext_platform/scripts/run_eval_small.py#L24)

### 8. `PrE-Text` 阶段间若不清理 GPU，也会叠加显存风险

- 问题现象
  - `stage1 -> stage2 -> eval_small` 如果在同一进程中连续执行且不做释放，理论上会把前一阶段残留模型和下一阶段模型叠加到 GPU 上，增加 OOM 风险。
- 根本原因
  - `PrE-Text` 是多阶段流水线，Stage 1、Stage 2、评估阶段都可能在运行中加载模型。
  - 单卡 `22GB` 环境下，阶段切换必须显式清理。
- 解决方式
  - `run_pipeline()` 在每个已完成阶段后调用 `release_gpu_memory()`。
  - 正式实验进一步拆为两步：
    1. `run_pipeline` 只做 `stage1 + stage2`
    2. `run_eval_small` 独立作为新进程执行
  - 这样即便阶段内释放不彻底，进程边界也能兜底清空模型对象。
- 当前状态
  - 已在代码与正式文档中对齐。
- 证据来源
  - [pipeline.py](/d:/学习记录/导师项目/研究/caiqiyue_file/PrE-Text/pretext_platform/core/pipeline.py#L183)
  - [resource_cleanup.py](/d:/学习记录/导师项目/研究/caiqiyue_file/PrE-Text/pretext_platform/core/resource_cleanup.py#L7)
  - [最终正式实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/最终正式实验设计.md#L39)
  - [服务器资源.md](/d:/学习记录/导师项目/研究/caiqiyue_file/服务器资源.md#L185)

### 9. Linux 服务器上的路径位置曾是实际风险点

- 问题现象
  - 如果 Linux 服务器上的 `datasets/`、`open_model/` 不在配置假定位置，会出现 `FileNotFoundError`。
- 根本原因
  - `PrE-Text` 配置依赖相对路径解析；只有当服务器目录结构与配置继承链一致时才能正确找到资源。
  - 审核文档明确指出：若服务器数据集实际挂载路径不同，需要修改路径或建立软链接。
- 解决方式
  - 核对并固定以下路径：
    - `../thesis_platform/datasets`
    - `../thesis_platform/open_model`
  - 若服务器实际路径不同，则：
    - 覆盖 `paths.dataset_root`
    - 或建立符号链接
- 当前状态
  - 路径解析机制本身正确；风险来自部署环境与配置不一致。
- 证据来源
  - [PrE-Text/docs/20260402_PrE-Text实验完整性最终审核报告.md](/d:/学习记录/导师项目/研究/caiqiyue_file/PrE-Text/docs/20260402_PrE-Text实验完整性最终审核报告.md#L417)
  - [PrE-Text/docs/20260402_PrE-Text源码最终完整性审核报告.md](/d:/学习记录/导师项目/研究/caiqiyue_file/PrE-Text/docs/20260402_PrE-Text源码最终完整性审核报告.md#L98)

### 10. GLUE 评估存在对外网或本地缓存的依赖

- 问题现象
  - `PrE-Text` 的 GLUE 评估在找不到本地 Arrow 数据时，会 fallback 到 HuggingFace 下载；无网络环境下会失败。
- 根本原因
  - `_load_local_glue_dataset()` 找不到本地缓存时，会走在线下载分支。
- 解决方式
  - 正式运行前提前检查本地 GLUE 数据集目录是否完整。
  - 无网环境不要把 GLUE 在线 fallback 当成可用路径。
- 当前状态
  - 属于低风险已知问题，不影响主实验，但影响 GLUE 附加评估。
- 证据来源
  - [PrE-Text/docs/20260402_PrE-Text源码最终完整性审核报告.md](/d:/学习记录/导师项目/研究/caiqiyue_file/PrE-Text/docs/20260402_PrE-Text源码最终完整性审核报告.md#L525)

### 11. 批量脚本的 `parallel` 语义曾被高估

- 问题现象
  - 文档里曾提供批量执行和 `parallel` 参数，但审核报告指出该能力并非完全按“可安全并行正式实验”实现。
- 根本原因
  - 在单卡 `22GB` 环境下，并行并不是默认安全策略。
  - 审核报告中也明确列出“parallel 参数未实现”或“不应被当作正式并行能力使用”的风险。
- 解决方式
  - 正式实验改为批次化、串行化调度。
  - 高风险模型实验单独排队。
  - 不再把批量脚本的并行参数当作正式实验默认执行方式。
- 当前状态
  - 已通过正式实验设计规避。
- 证据来源
  - [PrE-Text/docs/20260402_PrE-Text源码最终完整性审核报告.md](/d:/学习记录/导师项目/研究/caiqiyue_file/PrE-Text/docs/20260402_PrE-Text源码最终完整性审核报告.md#L715)

## 三、2026-04-08 至 2026-04-09 新增问题（近期 Codex 会话）

### 12. CUDA IPC 状态不一致导致下游评估 OOM（CUDA 19.19GB 幽灵占用）

- 问题现象
  - FL 训练阶段结束后进入下游评估时，CUDA 报错显示 19.19GB 显存被 Process 372419 占用，但 `nvidia-smi` 显示无任何进程。
  - 反复调用 `torch.cuda.empty_cache()` 和 `torch.cuda.synchronize()` 无法清理该"幽灵占用"。
- 根本原因
  - FL 训练组件（`server_ctx.text_backend`、`client_contexts[*].text_backend`、`client_contexts[*].embedder`、各 adapter）仍然持有实际 GPU 对象。
  - 仅调用 `_clear_gpu_memory()`（清理缓存）不能释放这些对象本身持有的显存。
  - CUDA IPC（进程间通信）状态出现不一致，导致 PyTorch 认为有进程仍在使用显存，但系统层面已无对应进程。
- 解决方式
  - 在 `thesis_platform/core/experiment_runner.py` 中新增 `_release_fl_resources(...)` 方法。
  - 在下游评估前显式调用所有 FL 组件的 `release()` 方法，再执行 `_clear_gpu_memory()`。
  - 流程变为：FL rounds 完成 → `_release_fl_resources()` → `_clear_gpu_memory()` → 进入下游评估。
- 当前状态
  - 代码已修复，但服务器端尚未同步更新。
- 证据来源
  - Codex 会话：`7aba7248-5711-4693-b291-ea058822b4ca`
  - 代码：
    - [experiment_runner.py L477](/d:/学习记录/导师项目/研究/caiqiyue_file/thesis_platform/core/experiment_runner.py#L477) 新增 `_release_fl_resources()`
    - [resource_cleanup.py](/d:/学习记录/导师项目/研究/caiqiyue_file/thesis_platform/core/resource_cleanup.py#L7)

### 13. Config path 解析错误导致 repo_root 变成 "/"

- 问题现象
  - 从 `run_downstream_eval.py` 独立运行时，`ExperimentConfig` 加载的 `repo_root` 被解析为 "/"（根目录），导致后续路径全部错误。
- 根本原因
  - `run_downstream_eval.py` 使用 `resolved_config.json` 中的 `config_path` 构造 `ExperimentConfig`，但传入的 `path` 是时间戳目录而非实际配置文件路径。
  - `config.repo_root()` 基于传入的 path 向上查找，找到的根目录不正确。
- 解决方式
  - 使用 `resolved_config.json` 中记录的原始 `config_path`（而非 `resolved_config.json` 本身的位置）来构造 `ExperimentConfig`。
  - 修复后 `run_downstream_eval.py` 可正确找到模型和数据路径。
- 当前状态
  - 已在 `run_downstream_eval.py` 中修复。
- 证据来源
  - Codex 会话：`7aba7248-5711-4693-b291-ea058822b4ca`
  - 代码：
    - [run_downstream_eval.py L57-65](/d:/学习记录/导师项目/研究/caiqiyue_file/thesis_platform/scripts/run_downstream_eval.py#L57-L65)

### 14. DownstreamEvalManager 重用失败的结果文件导致幽灵结果

- 问题现象
  - 下游评估失败后，重新运行时 `_reuse_existing_stage_payload()` 读取了之前失败留下的 summary 文件。
  - 导致最终结果看起来"成功"，但实际评估未重新运行。
- 根本原因
  - `_reuse_existing_stage_payload()` 设计为跳过已完成的阶段，但未区分"成功完成"和"失败遗留"的 summary 文件。
- 解决方式
  - 在重新运行下游评估前，删除旧的 summary 文件（`downstream_eval_summary.json`、`pretext_small_eval_summary.json` 等）。
  - 强制重新执行评估逻辑。
- 当前状态
  - 已通过手动清理旧文件解决，代码层面尚未永久修复。
- 证据来源
  - Codex 会话：`7aba7248-5711-4693-b291-ea058822b4ca`
  - 代码：
    - [downstream_eval.py](/d:/学习记录/导师项目/研究/caiqiyue_file/thesis_platform/evaluation/downstream_eval.py)

### 15. SSH 连接失败（SSH_ASKPASS 问题）

- 问题现象
  - 使用 `sshpass -p password ssh ...` 直接在命令中传递密码，导致 SSH 连接失败。
  - 错误原因：SSH 默认禁止在命令行直接传递密码。
- 根本原因
  - SSH 客户端安全策略不允许 `-p password` 形式传递密码，需要通过 `SSH_ASKPASS` 或其他机制。
- 解决方式
  - 使用 C# 编译的辅助程序 `sshaskpass.exe` 来处理密码输入。
  - 或使用 Expect 脚本交互式传递密码。
- 当前状态
  - 已找到替代方案。
- 证据来源
  - Codex 会话：`7aba7248-5711-4693-b291-ea058822b4ca`

### 16. 隐私预算耗尽导致下游性能下降（实验结论层面的问题）

- 问题现象
  - GC1（DataInf 选择）和 GA6（随机选择）的下游评估 `best_top1` 均低于 baseline：
    - GC1: 0.2684（baseline 0.2813, -4.6%）
    - GA6: 0.2780（baseline 0.2813, -1.2%）
  - 两者的 `privacy_spent_cumulative` 均为 ~4.17，但 epsilon=1.29，说明隐私预算已严重超支。
- 根本原因
  - 差分隐私预算设计存在缺陷：每轮联邦学习都在消耗隐私预算，但累积 10 轮后总预算远超初始设定。
  - 这导致差分隐私噪声叠加过大，合成语料质量严重下降。
- 解决方式
  - 待验证：运行 GP4（无隐私约束）确认是否是隐私预算耗尽导致性能下降。
  - 若确认，则需要重新设计隐私预算分配策略（如增大 epsilon 或减少 rounds）。
- 当前状态
  - 待 GP4 实验结果验证。
- 证据来源
  - Codex 会话：`7aba7248-5711-4693-b291-ea058822b4ca`
  - GA6 结果：`jobs_real_datainf_v3_ga6_random_sel_linux_20260408_001356/metrics_summary.json`
  - GC1 结果：`jobs_real_datainf_v3_linux_20260408_044041/metrics_summary.json`

## 四、结论

近期 Codex 会话中，两个项目真正有价值的问题并不多，核心集中在三类：

- `GPU 显存释放不彻底`：这是最直接、最危险的问题，已经通过代码清理和执行阶段拆分修复。
- `正式实验口径漂移`：主要体现在 `PrE-Text` 的 `eval_small` 与环境/backend 选择上，现已统一。
- `部署环境与执行方式不一致`：包括路径、`vllm` 环境、GLUE 依赖、批量并行误解，这些本质上是实验工程管理问题，不是算法本体问题。

**2026-04-08 至 2026-04-09 新增问题（问题 12-16）：**
- 问题 12：CUDA IPC 状态不一致导致幽灵显存占用，代码修复已完成但服务器未同步
- 问题 13：Config path 解析导致 repo_root 为 "/"，`run_downstream_eval.py` 已修复
- 问题 14：下游评估重用失败文件导致幽灵结果，手动清理解决
- 问题 15：SSH ASKPASS 连接问题，已找到替代方案
- 问题 16：隐私预算耗尽导致性能下降（实验结论），待 GP4 验证

当前状态下，只要严格按照 [整体实验设计.md](/d:/学习记录/导师项目/研究/caiqiyue_file/整体实验设计.md) 和 [服务器资源.md](/d:/学习记录/导师项目/研究/caiqiyue_file/服务器资源.md) 的执行口径运行，近期会话里暴露出的关键问题已经基本被覆盖。
