inherits:
  - ../base/paths.yaml
  - ../base/models.yaml
  - ../base/runtime.yaml
  - ../templates/noise_eps758.yaml

meta:
  experiment_id: smoke_bioarxiv_eps758
  seed: 42

data:
  dataset_name: bioarxiv
  max_samples_per_client: 8
  initialization_min_words: 20

stage1:
  rounds: 2
  lookahead: 1
  multiplier: 1

bootstrap:
  num_prompts: 16

eval_small:
  enabled: false

eval_large:
  enabled: true
  batch_size: 2
  eval_batch_size: 1
