# 整体实验设计：小数据全流程测试实验

## 1. 文档目的

本文档基于当前两个仓库的实际源码与现有配置，挑选若干组可以用较小数据集跑通的测试性实验，用于验证：

- `thesis_platform` 创新算法主链路是否能从配置解析一路执行到主实验结果与下游评测结果。
- `PrE-Text` 对照算法是否能从 `stage1`、`stage2` 一路执行到 `eval_small` 与 `eval_large`。
- 合成语料是否能继续进入 GLUE 类下游任务，证明整个实验闭环是完整的。

这些实验的目标是“证明平台完整性与正确性”，不是论文正式结果实验，因此故意使用了更小的 `train_limit`、`eval_limit`、`rounds`、`num_prompts` 等配置。

## 2. 结论

基于当前源码，推荐执行以下 5 组测试性实验：

1. `TP-1`：创新算法最小一条龙完整性实验。
2. `TP-2`：创新算法 Linux 正式路径微型实验。
3. `TP-3`：创新算法合成语料的 GLUE 衍生验证。
4. `PT-1`：PrE-Text 最小完整全流程实验。
5. `PT-2`：PrE-Text 与论文主数据路径一致的微型实验。

这 5 组实验覆盖了：

- 创新算法主实验。
- 创新算法独立下游评测。
- 创新算法合成语料进入 GLUE 分类评测。
- PrE-Text 的 `stage1`、`stage2`、`eval_small`、`eval_large` 全流程。

## 3. 执行前提

- 已安装两个仓库各自的 `requirements.txt` 依赖。
- 本地模型目录和数据目录保持当前仓库默认结构，不额外改路径。
- 命令以 PowerShell 为例。
- `thesis_platform` 的命令统一从工作区根目录执行：
  `D:\学习记录\导师项目\研究\caiqiyue_file`
- `PrE-Text` 的命令统一从其仓库根目录执行：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text`

## 4. 输出位置规则

### 4.1 thesis_platform

`thesis_platform` 的实验输出根目录固定为：

`D:\学习记录\导师项目\研究\caiqiyue_file\outputs\thesis_platform`

其输出规则有两个层次：

- 最新运行指针文件：
  `D:\学习记录\导师项目\研究\caiqiyue_file\outputs\thesis_platform\<experiment_id>_latest.json`
- 实际运行目录：
  `latest.json` 中 `experiment_dir` 字段指向的时间戳目录，例如  
  `D:\学习记录\导师项目\研究\caiqiyue_file\outputs\thesis_platform\jobs_v3_tiny_linux_20260403_101530`

因此，`thesis_platform` 的过程与结果都应该优先按“先看 latest 指针，再看真实目录”的方式定位。

### 4.2 PrE-Text

`PrE-Text` 的实验输出根目录固定为：

`D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform`

其输出规则更简单，直接按实验 ID 建目录：

`D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\<experiment_id>`

因此，`PrE-Text` 的过程与结果位置是固定目录，不需要再解析 latest 指针。

## 5. 推荐执行顺序

建议按下面顺序执行：

1. `TP-1`
2. `TP-2`
3. `TP-3`
4. `PT-1`
5. `PT-2`

这样可以先验证创新算法最小链路，再验证与正式 Linux 路径一致的微型版本，最后验证 PrE-Text 对照链路。

## 6. 测试性实验清单

### 6.1 TP-1：创新算法最小一条龙完整性实验

**实验命令**

```powershell
Set-Location D:\学习记录\导师项目\研究\caiqiyue_file
python -m thesis_platform.scripts.run_experiment --config thesis_platform/configs/experiments/validation/integrity_jobs_real_datainf_qwen_full_eval.yaml
```

**命令含义**

- 使用 `jobs` 数据集的极小样本配置。
- 只跑 `1` 轮主实验，客户端和服务端都使用 `qwen_2_0_5b_instruct`。
- 配置中已启用 `downstream_eval`，因此这一条命令会完成：
  主实验 -> 合成语料导出 -> 下游大模型评测。

**为什么推荐它**

- 这是当前 `thesis_platform` 中最小、最直接、且自带下游评测的一条龙配置。
- 它最适合用来验证“创新算法是否能在小数据上完整闭环”。

**过程存储位置**

- latest 指针：
  `D:\学习记录\导师项目\研究\caiqiyue_file\outputs\thesis_platform\integrity_jobs_real_datainf_qwen_full_eval_latest.json`
- 主实验过程目录：
  `<TP-1_RUN>\round_000\`
- 下游评测过程目录：
  `<TP-1_RUN>\downstream_eval\`
- 合成语料目录：
  `<TP-1_RUN>\downstream_eval\stage2\`

其中 `<TP-1_RUN>` 表示 latest 指针里 `experiment_dir` 指向的真实运行目录。

**结果存储位置**

- 实验汇总：
  `<TP-1_RUN>\metrics_summary.json`
- 解析后的完整配置：
  `<TP-1_RUN>\resolved_config.json`
- 下游评测汇总：
  `<TP-1_RUN>\downstream_eval\downstream_eval_summary.json`
- 大模型评测汇总：
  `<TP-1_RUN>\downstream_eval\pretext_large_eval_summary.json`
- 导出的合成语料：
  `<TP-1_RUN>\downstream_eval\stage2\llama7b_text_syn.json`

**证明点**

- 创新算法主链路可以从配置解析一路跑到主实验结果。
- 主实验输出的合成语料可以被下游评测模块继续消费。
- 最终能同时得到主实验汇总和下游评测汇总。

### 6.2 TP-2：创新算法 Linux 正式路径微型实验

**实验命令**

```powershell
Set-Location D:\学习记录\导师项目\研究\caiqiyue_file
python -m thesis_platform.scripts.run_experiment --config thesis_platform/configs/experiments/linux/jobs_v3_tiny_linux.yaml
python -m thesis_platform.scripts.run_downstream_eval --experiment-id jobs_v3_tiny_linux --large-eval-mode gpt2_xl
```

**命令含义**

- 第 1 条命令运行 `jobs_v3_tiny_linux`，它是与 Linux 正式实验路径一致的微型版本。
- 该配置使用更小的 `train_limit=8`、`eval_limit=8`、`rounds=2`。
- 第 2 条命令在主实验完成后，读取 `jobs_v3_tiny_linux_latest.json` 指向的最近一次运行目录，补跑下游大模型评测。
- `gpt2_xl` 是轻量化大模型评测模式，适合作为测试闭环验证。

**为什么推荐它**

- 它使用的是 `linux` 目录下的真实实验路径，而不是单纯的本地完整性样例。
- 它更接近正式论文实验的代码走法，但规模更小。

**过程存储位置**

- latest 指针：
  `D:\学习记录\导师项目\研究\caiqiyue_file\outputs\thesis_platform\jobs_v3_tiny_linux_latest.json`
- 主实验轮次目录：
  `<TP-2_RUN>\round_000\`
- 主实验轮次目录：
  `<TP-2_RUN>\round_001\`
- 下游评测过程目录：
  `<TP-2_RUN>\downstream_eval\`
- 合成语料目录：
  `<TP-2_RUN>\downstream_eval\stage2\`

其中 `<TP-2_RUN>` 表示 latest 指针里 `experiment_dir` 指向的真实运行目录。

**结果存储位置**

- 主实验汇总：
  `<TP-2_RUN>\metrics_summary.json`
- 解析后的完整配置：
  `<TP-2_RUN>\resolved_config.json`
- 下游评测汇总：
  `<TP-2_RUN>\downstream_eval\downstream_eval_summary.json`
- 大模型评测汇总：
  `<TP-2_RUN>\downstream_eval\pretext_large_eval_summary.json`
- 导出的合成语料：
  `<TP-2_RUN>\downstream_eval\stage2\llama7b_text_syn.json`

**证明点**

- 创新算法的 Linux 正式实验路径可以用小数据完整跑通。
- 主实验和独立下游评测两个入口可以顺利衔接。
- 真实时间戳目录、latest 指针、下游目录三者关系明确且稳定。

### 6.3 TP-3：创新算法合成语料的 GLUE 衍生验证

**实验命令**

```powershell
Set-Location D:\学习记录\导师项目\研究\caiqiyue_file
python -m thesis_platform.scripts.run_glue_eval --experiment-id jobs_v3_tiny_linux --tasks sst2
```

**命令含义**

- 该命令读取 `TP-2` 生成的合成语料：
  `<TP-2_RUN>\downstream_eval\stage2\llama7b_text_syn.json`
- 然后对 `sst2` 任务执行一个最小 GLUE 风格分类评测。

**为什么推荐它**

- `sst2` 是最小、最容易快速验证的分类任务之一。
- 它可以证明创新算法生成的合成语料，能够继续进入衍生下游任务，而不只是停留在主实验内部。

**过程存储位置**

- GLUE 评测目录：
  `<TP-2_RUN>\glue_eval\glue_sst2_eval\`

**结果存储位置**

- 单任务汇总：
  `<TP-2_RUN>\glue_eval\glue_sst2_summary.json`
- GLUE 总汇总：
  `<TP-2_RUN>\glue_eval\glue_summary.json`

**证明点**

- 创新算法导出的合成语料不仅能用于大模型评测，也能进入轻量级分类评测。
- 实验闭环从“生成语料”延伸到了“衍生任务验证”。

### 6.4 PT-1：PrE-Text 最小完整全流程实验

**实验命令**

```powershell
Set-Location D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text
python -m pretext_platform.scripts.run_pipeline --config configs/experiments/validate_tiny_complete_test.yaml
```

**命令含义**

- 使用 `jobs` 数据集的小样本配置。
- 该配置已开启：
  `stage1 + bootstrap(stage2) + eval_small + eval_large`
- 因此一条命令即可完成 PrE-Text 的完整主链路。

**为什么推荐它**

- 这是当前 `PrE-Text` 仓库中最适合做“完整性验证”的微型配置。
- 它显式启用了小模型评测和大模型评测，覆盖最全。

**过程存储位置**

- 实验目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\`
- Stage1 过程目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\stage1\`
- Stage2 过程目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\stage2\`
- 小模型评测过程目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\eval_small\`
- 大模型评测过程目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\eval_large\`

**结果存储位置**

- 实验汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\metrics_summary.json`
- 解析后的完整配置：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\resolved_config.json`
- Stage1 汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\stage1_summary.json`
- Stage2 汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\stage2_summary.json`
- 小模型评测汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\eval_small_summary.json`
- 大模型评测汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\eval_large_summary.json`
- 导出的合成语料：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_tiny_complete_test_windows\stage2\llama7b_text_syn.json`

**证明点**

- PrE-Text 的完整主流程可以在小数据上一次性跑通。
- `stage1`、`stage2`、`eval_small`、`eval_large` 的过程和结果目录都明确存在。
- 合成语料的输出位置固定且可复用。

### 6.5 PT-2：PrE-Text 与论文主数据路径一致的微型实验

**实验命令**

```powershell
Set-Location D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text
python -m pretext_platform.scripts.run_pipeline --config configs/experiments/validate_jobs_tiny_thesis_platform.yaml
```

**命令含义**

- 使用 `jobs` 数据集，并直接复用 `thesis_platform` 的数据目录与模型目录。
- 该配置会执行：
  `stage1 + bootstrap(stage2) + eval_large`
- 它比 `PT-1` 略重，但更接近论文主实验的数据路径组织方式。

**为什么推荐它**

- 这组实验可以验证 `PrE-Text` 与 `thesis_platform` 共享数据/模型路径时，流程仍然可正常闭环。
- 它更接近两套平台共同参与论文实验时的实际组织方式。

**过程存储位置**

- 实验目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\`
- Stage1 过程目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\stage1\`
- Stage2 过程目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\stage2\`
- 大模型评测过程目录：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\eval_large\`

**结果存储位置**

- 实验汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\metrics_summary.json`
- 解析后的完整配置：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\resolved_config.json`
- Stage1 汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\stage1_summary.json`
- Stage2 汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\stage2_summary.json`
- 大模型评测汇总：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\eval_large_summary.json`
- 导出的合成语料：
  `D:\学习记录\导师项目\研究\caiqiyue_file\PrE-Text\outputs\pretext_platform\validate_jobs_tiny_thesis_platform_eps758\stage2\llama7b_text_syn.json`

**证明点**

- PrE-Text 可以在与创新算法共享的数据和模型目录结构下完成真实链路。
- 这条链路适合作为论文正式对照实验之前的微型全流程验证。

## 7. 最小验收标准

以上每组测试性实验，建议统一按下面标准判定是否“全流程跑通”：

1. 命令退出码为 `0`。
2. 实验目录成功生成。
3. `resolved_config.json` 成功生成。
4. `metrics_summary.json` 成功生成。
5. 合成语料文件成功生成。
   - 创新算法：`downstream_eval\stage2\llama7b_text_syn.json`
   - PrE-Text：`stage2\llama7b_text_syn.json`
6. 对应的评测摘要文件成功生成。
   - 创新算法：`downstream_eval_summary.json`、`pretext_large_eval_summary.json`
   - PrE-Text：`eval_small_summary.json`、`eval_large_summary.json`
7. 若执行了 `TP-3`，则 `glue_summary.json` 成功生成。

## 8. 最终建议

如果只想先做一轮最小验证，优先执行下面两组：

1. `TP-1`
2. `PT-1`

如果这两组可以跑通，说明两个平台各自最小的完整闭环都已经成立。

如果还要进一步证明“与论文正式实验路径尽量一致”，再继续执行：

1. `TP-2`
2. `TP-3`
3. `PT-2`

这样可以在较小数据量下，基本证明当前源码已经具备支撑正式实验的完整性与正确性。
