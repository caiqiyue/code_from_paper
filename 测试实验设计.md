# 代码可行性测试实验设计

> 生成时间：2026-04-06
> 核心原则：只要以下测试实验全部通过，就代表代码层面没有任何问题，可以确保正式实验能够运行成功
> 适用环境：Linux 服务器（AutoDL RTX 2080 Ti, 22GB）

---

## 一、测试策略总览

### 1.1 核心思想

用**最小资源**验证**所有关键代码路径**：
- 每个测试实验只关注一个模块/组件
- 测试实验使用极小数据集（正式实验的 1/10 ~ 1/50）
- 测试实验使用与正式实验完全相同的配置结构
- 所有测试实验通过 = 代码链路无 bug = 正式实验可执行

### 1.2 测试通过标准

每个测试实验必须满足：
1. ✅ 命令能正常执行（无代码报错）
2. ✅ 生成预期的 `metrics_summary.json`
3. ✅ 过程文件完整（round_* 目录、stage1/、stage2/ 等）
4. ✅ 无显存溢出或 OOM
5. ✅ 合成语料文件正确生成

### 1.3 测试分组

| 编号 | 测试名称 | 验证内容 | 预计时间 |
|------|----------|----------|----------|
| TP-T1 | 端到端最小链路 | 完整流程 + 下游评测 + GLUE | ~30min |
| TP-T2 | Scorer多样性 | DataInf/GradMM/IRA 切换 | ~15min×3 |
| TP-T3 | Aggregator多样性 | dbscan/uid/summ 切换 | ~15min×3 |
| TP-T4 | Privacy梯度 | eps05/129/758/no_privacy | ~15min×4 |
| TP-T5 | Routing开关 | routing on/off 对比 | ~15min×2 |
| TP-T6 | 多轮联邦 | 3轮完整联邦迭代 | ~20min |
| PT-T1 | PrE-Text端到端 | stage1+stage2+eval_small | ~20min |
| PT-T2 | PrE-Text Privacy | eps05/129/758/no_privacy | ~15min×4 |
| PT-T3 | PrE-Text Bootstrap | llama2_7b bootstrap | ~10min |

---

## 二、thesis_platform 测试实验

### 2.1 执行环境

```bash
cd /root/autodl-tmp/caiqiyue/code_from_paper
conda activate caiqiyue
```

### 2.2 测试配置

#### TP-T1: 端到端最小链路

验证完整流程：**生成 → 评分 → 选择 → 召回 → 批评 → 聚合 → Prompt更新 → 下游评测 → GLUE**

```bash
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T1_end_to_end_minimal.yaml
```

**关键产物**：
- `TP_RUN(TP_T1_end_to_end_minimal)/metrics_summary.json`
- `TP_RUN(TP_T1_end_to_end_minimal)/downstream_eval/downstream_eval_summary.json`
- `TP_RUN(TP_T1_end_to_end_minimal)/glue_eval/glue_sst2_summary.json`

**配置内容** `TP_T1_end_to_end_minimal.yaml`：

```yaml
# TP-T1: 端到端最小链路测试
# 验证完整流程: 生成→评分→选择→召回→批评→聚合→Prompt更新→下游评测→GLUE
inherits:
  - ../../base/paths.yaml
  - ../../base/runtime.yaml
  - ../../base/llm_3b_linux.yaml
  - ../../methods/generators/pretext_prompt_llm.yaml
  - ../../methods/scorers/datainf_real.yaml
  - ../../methods/retrievers/knn.yaml
  - ../../methods/critics/fedtextgrad_qwen.yaml
  - ../../methods/aggregators/dbscan_attn_tsgdm.yaml
  - ../../methods/prototypes/minilm_mean.yaml
  - ../../methods/routing/personalized_v3.yaml
  - ../../methods/privacy/jobs_eps129.yaml
  - ../../methods/downstream_eval/pretext_large_off.yaml

meta:
  experiment_id: TP_T1_end_to_end_minimal
  stage: validation
  seed: 42

data:
  dataset_name: jobs
  task_type: instruction_tuning
  sample_format: raw_text
  partition_strategy: preserve_buckets
  train_path: thesis_platform/datasets/pretext_jobs/formatted/jobs_train.json
  eval_path: thesis_platform/datasets/pretext_jobs/formatted/jobs_eval.json
  public_seed_path: thesis_platform/datasets/pretext_initialization_c4_en/formatted/initialization.json
  initialization_path: thesis_platform/datasets/pretext_initialization_c4_en/formatted/initialization.json
  initial_prompt_path: ""
  max_public_seed_samples: 8
  num_clients: 4
  max_samples_per_client: 4
  validation_ratio: 0.25
  initialization_min_words: 20
  train_limit: 16
  eval_limit: 8

generator:
  generated_per_round: 4
  exemplars_per_prompt: 1
  max_new_tokens: 64
  temperature: 0.2
  initial_prompt: "Generate job-related text."

retriever:
  embedding_model: thesis_platform/open_model/all_minilm_l6_v2
  allow_hashing_fallback: false
  top_k: 1

aggregator:
  name: dbscan_attn_tsgdm
  max_rules: 2
  embedding_model: thesis_platform/open_model/all_minilm_l6_v2
  allow_hashing_fallback: false
  cluster_eps: 0.35
  cluster_min_samples: 1
  momentum_beta: 0.7

federation:
  rounds: 1
  top_k_bad: 1
```

#### TP-T2: Scorer多样性测试

验证 GS1/GS2/GS3 所使用的三种 Scorer 都能正常工作。

```bash
# DataInf (GS1)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T2_datainf.yaml

# GradMM (GS2)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T2_gradmm.yaml

# IRA (GS3)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T2_ira.yaml
```

**关键产物**：
- `TP_RUN(TP_T2_datainf)/metrics_summary.json`
- `TP_RUN(TP_T2_gradmm)/metrics_summary.json`
- `TP_RUN(TP_T2_ira)/metrics_summary.json`

**TP-T2 通用基础配置** (TP_T2_datainf.yaml, TP_T2_gradmm.yaml, TP_T2_ira.yaml 各自继承)：

```yaml
# TP-T2: Scorer多样性测试基础配置
inherits:
  - ./TP_T1_end_to_end_minimal.yaml
  # 注意：替换下面这一行来切换 scorer
  - ../../methods/scorers/datainf_real.yaml  # datainf
  # - ../../methods/scorers/gradmm_real.yaml  # gradmm
  # - ../../methods/scorers/ira.yaml           # ira

meta:
  experiment_id: TP_T2_datainf  # 或 TP_T2_gradmm, TP_T2_ira
```

#### TP-T3: Aggregator多样性测试

验证 GAgg1~GAgg4 所使用的四种 Aggregator 都能正常工作。

```bash
# dbscan_attn_tsgdm (GAgg1, 默认)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T3_dbscan.yaml

# dbscan_attn (GAgg2)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T3_dbscan_attn.yaml

# uid (GAgg3)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T3_uid.yaml

# summarization (GAgg4)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T3_summ.yaml
```

#### TP-T4: Privacy梯度测试

验证 GP1/GP2/GP3/GP4 所使用的四种隐私配置都能正常工作。

```bash
# epsilon=0.5 (GP1)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T4_eps05.yaml

# epsilon=1.29 (GP2, 默认)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T4_eps129.yaml

# epsilon=7.58 (GP3)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T4_eps758.yaml

# no_privacy (GP4)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T4_no_privacy.yaml
```

#### TP-T5: Routing开关测试

验证 GA2 所使用的 routing on/off 切换。

```bash
# routing ON (personalized_v3)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T5_routing_on.yaml

# routing OFF (none)
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T5_routing_off.yaml
```

#### TP-T6: 多轮联邦迭代测试

验证多轮（3轮）联邦学习能正常迭代。

```bash
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T6_multiround.yaml
```

**关键产物**：
- `TP_RUN(TP_T6_multiround)/round_000/`, `round_001/`, `round_002/`

```yaml
# TP-T6: 多轮联邦迭代测试
inherits:
  - ./TP_T1_end_to_end_minimal.yaml

meta:
  experiment_id: TP_T6_multiround

federation:
  rounds: 3
  top_k_bad: 2
```

#### TP-T7: GLUE串联测试

验证主实验完成后，GLUE评测能正确读取合成语料并执行。

```bash
# 先执行主实验（如果还没执行过）
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T1_end_to_end_minimal.yaml

# 再执行GLUE评测
python -m thesis_platform.scripts.run_glue_eval \
  --experiment-id TP_T1_end_to_end_minimal \
  --tasks sst2 qnli
```

**关键产物**：
- `TP_RUN(TP_T1_end_to_end_minimal)/glue_eval/glue_sst2_summary.json`
- `TP_RUN(TP_T1_end_to_end_minimal)/glue_eval/glue_qnli_summary.json`
- `TP_RUN(TP_T1_end_to_end_minimal)/glue_eval/glue_summary.json`

---

## 三、PrE-Text 测试实验

### 3.1 执行环境

```bash
cd /root/autodl-tmp/caiqiyue/code_from_paper/PrE-Text
conda activate caiqiyue
```

### 3.2 测试配置

#### PT-T1: PrE-Text 端到端最小链路

验证 PrE-Text 完整流程：**Stage1 → Stage2 → Eval_Small**

```bash
python -m pretext_platform.scripts.run_pipeline \
  --config configs/experiments/PT_T1_pretext_full_pipeline.yaml
```

**关键产物**：
- `PT_RUN(PT_T1_pretext_full_pipeline)/stage1_summary.json`
- `PT_RUN(PT_T1_pretext_full_pipeline)/stage2_summary.json`
- `PT_RUN(PT_T1_pretext_full_pipeline)/eval_small_summary.json`
- `PT_RUN(PT_T1_pretext_full_pipeline)/metrics_summary.json`

**PT-T1 配置** `PT_T1_pretext_full_pipeline.yaml`：

```yaml
# PT-T1: PrE-Text 端到端最小链路测试
inherits:
  - ../base/paths.yaml
  - ../base/models.yaml
  - ../base/runtime.yaml
  - ../templates/noise_eps758.yaml

meta:
  experiment_id: PT_T1_pretext_full_pipeline
  seed: 42

paths:
  dataset_root: ../thesis_platform/datasets
  model_root: ../thesis_platform/open_model

data:
  dataset_name: jobs
  train_path: ../thesis_platform/datasets/pretext_jobs/formatted/jobs_train.json
  eval_path: ../thesis_platform/datasets/pretext_jobs/formatted/jobs_eval.json
  initialization_path: ../thesis_platform/datasets/pretext_initialization_c4_en/formatted/initialization.json
  max_samples_per_client: 4
  initialization_min_words: 20
  train_limit: 32
  eval_limit: 16
  initialization_limit: 256

stage1:
  rounds: 2
  sensitivity: 8
  lookahead: 1
  multiplier: 1
  batch_size: 16
  embed_batch_size: 32
  nearest_neighbors_print: 1
  num_workers: 0

bootstrap:
  enabled: true
  num_prompts: 16
  generator_backend: huggingface
  generator_model: distilgpt2
  batch_size: 4
  device: cuda
  max_tokens: 64
  temperature: 1.0
  top_p: 1.0
  max_model_len: 512

eval_small:
  enabled: true
  eval_mode: gpt2
  cutoff_len: 64
  grad_accum_steps: 16
  epochs: 3
  batch_size: 16
  eval_batch_size: 8
  learning_rate: 0.0002

eval_large:
  enabled: false
```

#### PT-T2: PrE-Text Privacy梯度测试

```bash
# epsilon=0.5 (严格隐私)
python -m pretext_platform.scripts.run_pipeline \
  --config configs/experiments/PT_T2_eps05.yaml

# epsilon=1.29 (主基线)
python -m pretext_platform.scripts.run_pipeline \
  --config configs/experiments/PT_T2_eps129.yaml

# epsilon=7.58 (宽松隐私)
python -m pretext_platform.scripts.run_pipeline \
  --config configs/experiments/PT_T2_eps758.yaml

# no_privacy (无隐私约束)
python -m pretext_platform.scripts.run_pipeline \
  --config configs/experiments/PT_T2_no_privacy.yaml
```

#### PT-T3: PrE-Text Bootstrap llama2_7b测试

验证 `llama_2_7b_hf` bootstrap 阶段能正常工作。

```bash
python -m pretext_platform.scripts.run_pipeline \
  --config configs/experiments/PT_T3_llama2_7b_bootstrap.yaml
```

**PT-T3 配置** `PT_T3_llama2_7b_bootstrap.yaml`：

```yaml
# PT-T3: PrE-Text Bootstrap llama2_7b 测试
inherits:
  - ../base/paths.yaml
  - ../base/models.yaml
  - ../base/runtime.yaml
  - ../templates/noise_eps758.yaml

meta:
  experiment_id: PT_T3_llama2_7b_bootstrap
  seed: 42

paths:
  dataset_root: ../thesis_platform/datasets
  model_root: ../thesis_platform/open_model

data:
  dataset_name: jobs
  train_path: ../thesis_platform/datasets/pretext_jobs/formatted/jobs_train.json
  eval_path: ../thesis_platform/datasets/pretext_jobs/formatted/jobs_eval.json
  initialization_path: ../thesis_platform/datasets/pretext_initialization_c4_en/formatted/initialization.json
  max_samples_per_client: 4
  initialization_min_words: 20
  train_limit: 32
  eval_limit: 16
  initialization_limit: 256

stage1:
  rounds: 2
  sensitivity: 8
  lookahead: 1
  multiplier: 1
  batch_size: 16
  embed_batch_size: 32
  nearest_neighbors_print: 1
  num_workers: 0

bootstrap:
  enabled: true
  num_prompts: 32
  generator_backend: huggingface
  generator_model: llama2_7b  # 使用 llama_2_7b_hf
  batch_size: 1
  device: cuda
  max_tokens: 64
  temperature: 1.0
  top_p: 1.0
  max_model_len: 512

eval_small:
  enabled: true
  eval_mode: gpt2
  cutoff_len: 64
  grad_accum_steps: 16
  epochs: 3
  batch_size: 16
  eval_batch_size: 8
  learning_rate: 0.0002

eval_large:
  enabled: false
```

---

## 四、快速验收组合（必做）

如果时间紧迫，可只执行以下**最小验收组合**：

### 4.1 最小验收测试命令

```bash
# ===== thesis_platform =====
cd /root/autodl-tmp/caiqiyue/code_from_paper
conda activate caiqiyue

# T1: 端到端链路
python -m thesis_platform.scripts.run_experiment \
  --config thesis_platform/configs/experiments/validation/TP_T1_end_to_end_minimal.yaml

# T7: GLUE串联
python -m thesis_platform.scripts.run_glue_eval \
  --experiment-id TP_T1_end_to_end_minimal \
  --tasks sst2

# ===== PrE-Text =====
cd /root/autodl-tmp/caiqiyue/code_from_paper/PrE-Text
conda activate caiqiyue

# PT-T1: PrE-Text端到端
python -m pretext_platform.scripts.run_pipeline \
  --config configs/experiments/PT_T1_pretext_full_pipeline.yaml
```

### 4.2 验收标准

| 测试 | 预期产物 | 通过条件 |
|------|----------|----------|
| TP-T1 | `metrics_summary.json` | 文件存在且包含有效指标 |
| TP-T1 | `downstream_eval/downstream_eval_summary.json` | 文件存在 |
| TP-T1 | `downstream_eval/stage2/llama7b_text_syn.json` | 文件存在且非空 |
| TP-T7 | `glue_eval/glue_sst2_summary.json` | 文件存在 |
| PT-T1 | `stage1_summary.json` | 文件存在 |
| PT-T1 | `stage2_summary.json` | 文件存在 |
| PT-T1 | `eval_small_summary.json` | 文件存在 |
| PT-T1 | `metrics_summary.json` | 文件存在 |

---

## 五、正式实验启动前检查清单

在正式实验开始前，请确认：

- [ ] 所有测试实验（TP-T1 ~ TP-T7, PT-T1 ~ PT-T3）均已通过
- [ ] `outputs/thesis_platform/` 目录下有 `_latest.json` 指针文件
- [ ] `outputs/pretext_platform/` 目录下有对应的实验目录
- [ ] 所有模型权重文件已下载到 `open_model/` 目录
- [ ] 所有数据集文件已下载到 `datasets/` 目录
- [ ] GPU显存监控正常，无泄漏

---

## 六、测试配置快速创建脚本

如需批量创建测试配置，可参考以下模板生成：

```bash
# 在 thesis_platform/configs/experiments/validation/ 下创建以下文件：
# - TP_T1_end_to_end_minimal.yaml
# - TP_T2_datainf.yaml, TP_T2_gradmm.yaml, TP_T2_ira.yaml
# - TP_T3_dbscan.yaml, TP_T3_dbscan_attn.yaml, TP_T3_uid.yaml, TP_T3_summ.yaml
# - TP_T4_eps05.yaml, TP_T4_eps129.yaml, TP_T4_eps758.yaml, TP_T4_no_privacy.yaml
# - TP_T5_routing_on.yaml, TP_T5_routing_off.yaml
# - TP_T6_multiround.yaml

# 在 PrE-Text/configs/experiments/ 下创建以下文件：
# - PT_T1_pretext_full_pipeline.yaml
# - PT_T2_eps05.yaml, PT_T2_eps129.yaml, PT_T2_eps758.yaml, PT_T2_no_privacy.yaml
# - PT_T3_llama2_7b_bootstrap.yaml
```

---

## 七、故障排查

| 症状 | 可能原因 | 解决方案 |
|------|----------|----------|
| OOM | 显存不足 | 减少 `batch_size`、`max_samples_per_client` |
| CUDA out of memory | 模型太大 | 确认使用 `pretext_large_off.yaml` |
| File not found | 路径解析错误 | 检查 `repo_root` 配置 |
| metrics_summary.json 缺失 | 实验中途失败 | 查看 `logs/` 或 stdout |
| GLUE 评测找不到语料 | 阶段顺序错误 | 先执行 `run_experiment` 再执行 `run_glue_eval` |
