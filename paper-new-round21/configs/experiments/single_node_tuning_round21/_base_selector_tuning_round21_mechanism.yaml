inherits:
  - ../single_node_tuning_round19/_base_selector_tuning_round19.yaml

meta:
  stage: single_node_tuning_round21_mechanism
  seed: 42

paths:
  output_root: paper-new-round21/outputs/r21_mechanism_default

llm:
  generator:
    gpu_memory_utilization: 0.40
    startup_required_free_gb: 2

bootstrap:
  gpu_memory_utilization: 0.40
  startup_required_free_gb: 2

selector:
  seed_budget_rule:
    policies:
      uncertain:
        mode: policy_arbitration
        arbitration_enabled: true
        coverage_epsilon: 0.002
        support_epsilon: 0.002
        stability_epsilon: 0.002
        prefer_smaller_budget_on_tie: true
