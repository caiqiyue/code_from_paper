inherits:
  - ../_base_selector_tuning_round21.yaml
  - ../../single_node_tuning_round19/_data_congressional.yaml

meta:
  experiment_id: r21_probe_congressional_delta080_seed42
  seed: 42

paths:
  output_root: paper-new-round21/outputs/r21_probes/r21_probe_congressional_delta080_seed42

selector:
  seed_budget_rule:
    router:
      delta_router: 0.80
    policies:
      uncertain:
        arbitration_enabled: false
