# 服务器资源

更新时间：2026-04-09

本文档用于给实验设计、环境选择和参数设置提供可直接引用的服务器资源依据。  
详细原始采集记录保存在：

- `outputs/server_inventory_20260409/server_inventory_20260409.md`
- `outputs/server_inventory_20260409/raw_remote_inventory.txt`

## 1. 服务器概况

- 主机名：`autodl-container-91d74fb88e-2bbb3f11`
- 系统：`Linux 5.15.0-94-generic`
- 项目根目录：`/root/autodl-tmp/caiqiyue/code_from_paper`

## 2. 硬件资源

### 2.1 GPU

- 型号：`NVIDIA GeForce RTX 2080 Ti`
- 显存：`22528 MiB`
- 驱动版本：`550.120`
- CUDA 版本：`12.4`
- 当前模式：`Persistence-M On`

实验含义：

- 这是一张单卡 `22GB` 显存 GPU。
- 常规正式实验可以稳定支撑：
  - `thesis_platform` 主链
  - `thesis_platform` 小评估
  - `PrE-Text` Stage 1
  - `PrE-Text` Stage 2 的 `vllm` 生成
  - `PrE-Text` 的 `GPT-2` 小评估
- 高风险项：
  - `7B/8B` 模型规模实验
  - `eval_large`
  - 大模型长上下文大 batch 同时加载

### 2.2 CPU

- 型号：`Intel(R) Xeon(R) Platinum 8255C CPU @ 2.50GHz`
- 逻辑核数：`96`
- Socket：`2`
- 每核线程数：`2`

实验含义：

- 数据加载、Embedding、检索、评估和多进程预处理都不会成为主要瓶颈。
- 正式实验的主要瓶颈仍然是 GPU 显存与生成速度，而不是 CPU。

### 2.3 内存

- 总内存：`503 GiB`
- 可用内存：约 `460 GiB`
- Swap：`0`

实验含义：

- 主内存非常充足。
- 正式实验无需因为主内存不足而压缩数据规模。
- 参数约束主要来自 GPU 显存，而不是系统内存。

## 3. 磁盘与目录空间

### 3.1 关键挂载点

- 系统盘 `/`：`30G`，当前约用 `15G`
- 大盘 `/autodl-pub`：`7.0T`
- 数据盘自动挂载：`/autodl-pub/data`：`4.0T`
- 共享内存 `/dev/shm`：`24G`

### 3.2 项目实际占用

- 总项目目录：`/root/autodl-tmp/caiqiyue/code_from_paper`，约 `117G`
- 创新算法目录：`/root/autodl-tmp/caiqiyue/code_from_paper/thesis_platform`，约 `88G`
- `PrE-Text` 目录：`/root/autodl-tmp/caiqiyue/code_from_paper/PrE-Text`，约 `25G`
- `pretext` 环境：约 `7.2G`
- `caiqiyue` 环境：约 `6.1G`

实验含义：

- 当前磁盘容量足够继续保存正式实验结果。
- 仍建议所有新结果继续保存在 `/root/autodl-tmp/caiqiyue/code_from_paper/outputs` 下，保持和现有结构一致。

## 4. 虚拟环境

### 4.1 `pretext`

- 路径：`/root/miniconda3/envs/pretext`
- Python：`3.10.20`
- 关键包：
  - `torch==2.1.2`
  - `transformers==4.38.2`
  - `vllm==0.3.3`
  - `sentence-transformers==2.5.1`
  - `opacus==1.4.1`

适用范围：

- 全部 `PrE-Text` 正式实验
- 原因：该环境中已安装 `vllm`，可支持 Stage 2 强制 `vllm` 生成

### 4.2 `caiqiyue`

- 路径：`/root/miniconda3/envs/caiqiyue`
- Python：`3.11.15`
- 关键包：
  - `torch==2.6.0+cu124`
  - `transformers==5.4.0`
  - `sentence-transformers==5.3.0`
  - `peft==0.18.1`
  - `opacus==1.5.4`
- 该环境中没有 `vllm`

适用范围：

- 全部 `thesis_platform` 正式实验
- 原因：创新算法主链不依赖 `vllm`

## 5. 本地模型资源

当前项目已有的关键本地模型目录：

- `thesis_platform/open_model/all_minilm_l6_v2`
- `thesis_platform/open_model/roberta_large`
- `thesis_platform/open_model/qwen_2_0_5b_instruct`
- `thesis_platform/open_model/llama_2_7b_hf`
- `thesis_platform/open_model/Meta-Llama-3-8B`
- `thesis_platform/open_model/llama_3_1_8b_instruct`
- `thesis_platform/open_model/distilgpt2`

说明：

- 当前没有单独的 `c4_checkpoint.pth`
- 因此 `PrE-Text` 的小评估口径统一为 `GPT-2`
- 不再把 `DistilGPT2 warm-start` 作为本轮正式实验口径

## 6. 对实验参数的约束含义

### 6.1 可以稳定支持的正式参数

创新算法：

- `rounds = 10`
- `num_clients = 16`
- `max_samples_per_client = 16`
- `generated_per_round = 32`
- `max_new_tokens = 192`
- `small_batch_size = 8`
- `small_eval_batch_size = 2`
- `small_grad_accum_steps = 8`

PrE-Text：

- `stage1.batch_size = 64`
- `stage1.embed_batch_size = 128`
- `bootstrap.generator_backend = vllm`
- `bootstrap.num_prompts = 10000`
- `eval_small.batch_size = 8`
- `eval_small.eval_batch_size = 2`
- `eval_small.grad_accum_steps = 8`
- `eval_small.eval_mode = gpt2`

### 6.2 不建议作为默认正式值的参数

- `bootstrap.num_prompts = 80000`
- `eval_large.enabled = true`
- `7B/8B` 模型实验和其他实验混排
- 在单次实验里同时保留多个大模型对象不释放

## 7. 资源层面的正式实验执行建议

- `thesis_platform` 正式实验统一在 `caiqiyue` 环境运行
- `PrE-Text` 正式实验统一在 `pretext` 环境运行
- `PrE-Text` 统一拆成两步：
  1. `stage1 + stage2`
  2. `run_eval_small`
- `GM2/GM3/GM4` 单独跑，不和其他实验并队
- `eval_large` 不纳入当前正式实验批次

### 7.1 显存释放与 OOM 控制口径

- `PrE-Text`：
  - `run_pipeline` 在 `stage1` 与 `stage2` 之间有显式 GPU 清理
  - `run_pipeline` 结束后会再次清理 GPU 缓存
  - `run_eval_small` 单独作为新进程启动，因此不会继承 `run_pipeline` 中残留的模型显存
- `thesis_platform`：
  - 联邦主流程结束后，会先释放主链中的生成器、评分器、检索器、批评器、聚合器以及上下文对象
  - 清理后才进入 downstream evaluation / cross-domain evaluation
- 因此当前正式实验的安全前提是：
  - 每个阶段完成后撤出上一阶段模型，再进入下一阶段
  - 不把多个大模型同时常驻在单卡 `22GB` 显存上
  - 高风险 `7B/8B` 实验必须串行独立执行

## 8. 一句话结论

这台服务器的核心特征是：

- 单卡 `RTX 2080 Ti 22GB`
- CPU 和内存都非常充足
- 正式实验的主要约束是 GPU 显存和大模型阶段时长

因此，当前最合理的正式实验策略是：

- 创新算法走 `caiqiyue`
- `PrE-Text` 走 `pretext`
- 小评估统一用 `GPT-2`
- `PrE-Text` 的 Stage 2 统一 `vllm + num_prompts=10000`
- 高风险 7B/8B 模型实验单独排队
