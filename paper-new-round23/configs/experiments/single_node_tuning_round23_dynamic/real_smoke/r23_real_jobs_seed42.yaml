inherits:
  - ../_base_selector_tuning_round23_dynamic.yaml
  - ../_data_jobs.yaml

meta:
  experiment_id: r23_real_jobs_seed42
  seed: 42
  dataset_name: jobs

paths:
  output_root: outputs/real_smoke/jobs/seed42

selector:
  seed_top_k: 20

round23_controller:
  source_env: round23_real_smoke
  context_family: natural
  reference_budget: 20
