inherits:
  - ../_base_selector_tuning_round23_dynamic.yaml
  - ../_data_jobs.yaml

meta:
  experiment_id: e1_c4only_smoke_jobs_seed42
  seed: 42
  dataset_name: jobs

paths:
  output_root: outputs/e1_c4only_all6_smoke/jobs/seed42

pipeline:
  stage1_mode: c4_only
  stage2_mode: pretext_bootstrap
  run_eval: true

selector:
  seed_top_k: 20
  seed_budget_rule:
    enabled: false

round23_controller:
  enabled: false
  source_env: e1_c4only_all6_smoke

c4only_baseline:
  enabled: true
  epsilon: inf
  source_env: e1_c4only_all6_smoke
