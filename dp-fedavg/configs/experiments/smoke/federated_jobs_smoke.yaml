inherits:
  - ../../base/runtime.yaml
  - ../../base/generation_vllm.yaml
  - ../../base/evaluation.yaml
  - ../../datasets/jobs.yaml
  - ../../algorithms/fedavg_dp_base.yaml

meta:
  experiment_id: federated_jobs_smoke

data:
  pseudo_num_clients: 4
  max_samples_per_client: 8
  train_limit: 24
  eval_limit: 16

generation:
  generated_per_round: 1
  exemplars_per_prompt: 1
  max_new_tokens: 32

paths:
  output_root: dp-fedavg/outputs/federated_jobs_smoke
