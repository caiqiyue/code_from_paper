algorithm:
  runner_mode: federated
  local_update: fedavg
  rounds: 3
  sampling:
    sample_rate: 0.5
    expected_clients: 4
  dp:
    enabled: true
    clip_mode: flat
    clip_norm: 1.0
    noise_multiplier: 0.8
    delta: 1.0e-5
  aggregation:
    estimator: fixed_denom
