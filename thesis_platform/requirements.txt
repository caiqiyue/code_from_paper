# Complete third-party dependency set used by thesis_platform itself.
#
# Notes:
# - `numpy` is retained because thesis_platform invokes
#   `DataInf/src/generate_sentence-math_datasets.py`, which imports NumPy.
# - `sentence-transformers` is included because thesis_platform/models/embedding.py
#   can use it directly when a local embedding model path is configured.
# - Full DataInf training/influence-analysis dependencies are intentionally not
#   merged here; use `DataInf/requirements.txt` separately for that workflow.

PyYAML>=6.0
datasets>=2.20
huggingface_hub>=0.24
numpy>=1.26
pandas>=2.0
sentence-transformers>=2.2
tqdm>=4.66
